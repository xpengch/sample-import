# Configuration
session:
  max_iterations: 10
  max_hypotheses: 5

hypotheses:
  min_confidence: 0.9
  reject_threshold: 0.05

testing:
  mock_mode: true

state:
  directory: ~/.chip-fault-locator
  auto_save: true
